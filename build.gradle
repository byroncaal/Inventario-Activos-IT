// Archivo de nivel raíz: aquí se declaran los plugins que pueden usar
// los módulos del proyecto (como "app"), sin especificar su versión otra vez.
plugins {
    alias(libs.plugins.android.application) apply false
}